# Sources — STT1700

Aucun fichier de cours n'est commité. Cette page liste seulement les chemins consultés
(hors dépôt) et ce qui a été retenu — pas de citation, pas de contenu recopié.

## État au 2026-09-17

`D:\Math\STT1700` est rempli (lot A) : 45 fichiers rapatriés de StudiUM (site
`STT1700-A-A26`, id 355495) + `INDEX.md` + `LIENS.md` + une page
`01 Plan de cours/StudiUM - structure STT1700-A-A26.md`. Le plan de cours **PDF**
manque encore (le lot A le retente), mais la page structure StudiUM contient déjà le
contenu utile d'un plan de cours (calendrier, contenu par semaine, évaluations) : ce
n'est donc plus une inconnue.

## Matière de l'intra 1 (7 octobre 2026) — confirmée par le matériel, pas une hypothèse

`01 Plan de cours/StudiUM - structure STT1700-A-A26.md` donne le calendrier semaine par
semaine :

| Semaine | Contenu | Évaluation |
|---|---|---|
| 1 | Section 1 : statistique descriptive, méthodes graphiques | — |
| 2 | Section 1 : mesures de tendance centrale et de dispersion | TP1 |
| 3 | Section 1 : covariance, corrélation, régression | TP2 |
| 4 | Section 2 : probabilités, définitions, axiomes, propriétés | TP3 |
| 5 | Sections 2-3 : probabilités conditionnelles, indépendance, variables aléatoires discrètes | TP4 |
| 6 | Section 3 : variables aléatoires binomiales et multinomiales | **Intra 1** |
| 7 | Section 4 : lois continues, TLC (après l'intra) | TP5 |

**L'intra 1 couvre donc exactement les sections 1, 2 et 3** (statistique descriptive,
probabilités, variables aléatoires discrètes) — la section 4 (lois continues) commence la
semaine suivant l'intra. Confirmé indépendamment par `02 Notes de cours/AideMemoire_intra1_STT1700.pdf`
(l'aide-mémoire fourni à l'examen liste : écart-type/variance, covariance, coefficient de
corrélation, droite des moindres carrés — rien sur les lois continues, chi-deux ou
l'inférence des sections 5+).

Chapitres 4 à 10 des notes (lois continues → régression linéaire simple en inférence)
restent hors intra 1, matière d'intra 2 (11 novembre) ou du final (15 décembre) — non
lus en détail pour ce jalon, `defaultOn: false`.

## Contenu retenu par thème (lu directement dans les PDF, plus une vérification agy)

- **`stt1700-descr` (Section 1, `02 Notes de cours/1. Statistiques descriptives/CoursSTT1700Sect1A26H27.pdf`
  + `.../CoursSTT1700Sect1SymetrieBoxplot.pdf`)** : variable qualitative/quantitative,
  diagrammes (bâtons, histogramme, tige-feuilles), mode/moyenne/médiane (médiane = valeur(s)
  centrale(s) selon la parité de $n$), variance $s^2=\sum(x_i-\bar x)^2/(n-1)$ et écart-type
  (dénominateur $n-1$, pas $n$), transformation affine ($s_y=|b|s_x$), cote $z=(x-\bar
  x)/s_x$, quartiles **par la méthode du cours** ($Q_1$ à la position $(n+1)/4$, $Q_3$ à
  $3(n+1)/4$ dans les statistiques d'ordre, interpolée si non entière — pas la méthode
  "médiane des deux moitiés" d'autres manuels), IQR $=Q_3-Q_1$, résumé à 5 chiffres,
  boîte à moustaches **modifiée** (moustaches jusqu'à $1.5\times\text{IQR}$, au-delà =
  points identifiés séparément — c'est la version testée à l'examen selon le cours),
  asymétrie gauche/droite, **règle empirique 68-95-99** (le cours dit explicitement
  "99", pas "99,7" comme d'autres manuels — à respecter tel quel), covariance
  $c_{xy}=\sum(x_i-\bar x)(y_i-\bar y)/(n-1)$, coefficient de corrélation
  $r_{xy}=c_{xy}/(s_xs_y)\in[-1,1]$ (invariant par changement d'échelle, $|r_{xy}|=1$ ssi
  lien linéaire exact), droite des moindres carrés $\hat y=a+bx$ avec
  $b=\sum(x_i-\bar x)(y_i-\bar y)/\sum(x_i-\bar x)^2$ (covariance sur variance de $x$),
  $a=\bar y-b\bar x$, passe par $(\bar x,\bar y)$, et $r_{xy}=b\,s_x/s_y$ (même signe).
- **`stt1700-prob` (Section 2, `02 Notes de cours/2. Probabilités/CoursSTT1700Sect2A26H27.pdf`)** :
  espace échantillonnal, événements, union/intersection/complément, De Morgan, axiomes de
  Kolmogorov, $\Pr(A\cup B)=\Pr(A)+\Pr(B)-\Pr(A\cap B)$, équiprobabilité, probabilité
  conditionnelle $\Pr(A\mid B)=\Pr(A\cap B)/\Pr(B)$, règle de multiplication, probabilités
  totales, théorème de Bayes, indépendance $\Pr(A\cap B)=\Pr(A)\Pr(B)$ (distincte de
  l'incompatibilité), tirages avec/sans remise.
- **`stt1700-var` (Section 3, `02 Notes de cours/3. Variables Aléatoires/CoursSTT1700Sect3H26.pdf`)** :
  v.a. discrète, fonction de masse $p(x)=\Pr(X=x)$, fonction de répartition en escalier,
  espérance $E(X)=\sum xp(x)$, variance $\mathrm{var}(X)=E(X^2)-\mu^2$, transformation
  affine, combinaisons de v.a. (linéarité de $E$, $\mathrm{var}(\sum a_iX_i)=\sum
  a_i^2\mathrm{var}(X_i)$ sous indépendance, $\mathrm{var}(X+Y)\ne\mathrm{var}(X)+\mathrm{var}(Y)$
  en général), loi binomiale $X\sim\mathrm{Bin}(n,p)$ ($p(x)=\binom nx p^x(1-p)^{n-x}$,
  $E(X)=np$, $\mathrm{var}(X)=np(1-p)$), loi multinomiale (généralisation à $K$ classes,
  marginales binomiales).

Vérification : les formules de la loi binomiale (fonction de masse, $E(X)=np$,
$\mathrm{var}(X)=npq$) et le formulaire des moindres carrés ont été relus directement page
par page (extraction `pypdf`) en plus du passage par `agy` (Gemini) qui a d'abord mappé les
sous-thèmes des 3 sections avec citations `fichier:page` — le contenu ci-dessus est
celui confirmé par cette relecture directe, pas seulement rapporté par agy.

## Thèmes retenus et `defaultOn`

Les 10 sections des notes de cours donnent directement 10 thèmes (aucun id inventé au-delà
de ce que StudiUM liste). `defaultOn: true` pour les 3 thèmes de l'intra 1, comme demandé
par le brief tour 2 — **écart signalé** : le contrat de `defaultOn` dans `src/lib/types.ts`
dit "déjà vu en classe à la date du build" (ex. MAT1600 n'a que 3 de ses 5 thèmes intra
`defaultOn: true` tant que les 2 derniers ne sont pas enseignés). Au 2026-09-17 on ne sait
pas avec certitude combien de semaines de STT1700 sont réellement passées (le calendrier
donne des semaines de cours, pas de dates de calendrier civil pour chaque semaine), donc
appliquer la règle stricte aurait exigé une hypothèse supplémentaire non vérifiée. Le brief
demandait explicitement `defaultOn` vrai pour l'intra ; suivi tel quel, mais à trancher par
l'admin si la sémantique stricte doit s'appliquer aussi ici.

| id | libellé | section | examen | defaultOn |
|---|---|---|---|---|
| `stt1700-descr` | Statistique descriptive, corrélation et droite des moindres carrés | 1 | intra | `true` |
| `stt1700-prob` | Probabilités, conditionnelle et indépendance | 2 | intra | `true` |
| `stt1700-var` | Variables aléatoires discrètes, binomiale et multinomiale | 3 | intra | `true` |
| `stt1700-cont` | Lois continues et théorème limite central | 4 | final | `false` |
| `stt1700-est` | Estimations ponctuelles | 5 | final | `false` |
| `stt1700-ic` | Intervalles de confiance pour grands échantillons | 6 | final | `false` |
| `stt1700-test` | Tests d'hypothèses pour grands échantillons | 7 | final | `false` |
| `stt1700-petit` | Inférence pour petits échantillons | 8 | final | `false` |
| `stt1700-khi` | Tests de khi-deux | 9 | final | `false` |
| `stt1700-reg` | Régression linéaire simple (inférence : tests et IC sur la pente) | 10 | final | `false` |

`stt1700-reg` (section 10, régression **en inférence**) est délibérément distinct de la
droite des moindres carrés vue en descriptive (`stt1700-descr`, section 1) : même outil,
mais section 10 y ajoute tests d'hypothèses et intervalles de confiance sur les
paramètres — non lu en détail (hors intra 1), le libellé le précise pour éviter la
confusion au moment d'écrire des questions dessus plus tard.

## Jalon 1 (ce tour)

10 questions sur `stt1700-descr` (premier thème enseigné). Brouillon initial tenté via
`agy` (Gemini, quota Google) à partir de ce document et de `docs/PROCESSUS_QUESTIONS.md` ;
mesure du taux de questions gardées dans `docs/WORKLOG.md`.

## À refaire

- Lire le plan de cours PDF une fois que le lot A l'aura récupéré (peut préciser les dates
  civiles des semaines, ce qui trancherait le `defaultOn` strict ci-dessus).
- Lire les sections 4 à 10 en détail avant d'écrire leurs questions (intra 2 / final).
